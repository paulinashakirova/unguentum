import React from 'react';

export default function ExtraInfoBox() {
  return <div></div>;
}

//react-bootstrap
// modal with grid
//it has to be a function that should be triggered when i hover
//https://react-bootstrap.github.io/components/modal/
//( if i wont figure how to use hover and choose onClick, then i want to make sure that the user can escape that by clicking anywhere outside the borders)
//onHover{show(bootstrap property)}
//how can i combine card in bootstrap with modal and to add an image(obviously) what i want is 'Kitchen sink',
